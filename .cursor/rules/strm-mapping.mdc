---
description: NIST IR 8477 Set-Theory Relationship Mapping (STRM) rules. Apply when the user asks to map, crosswalk, align, compare, or gap-analyze cybersecurity frameworks, control catalogs, or regulatory requirements, or when producing STRM CSV output files.
globs: ["working-directory/**/*.csv", "working-directory/**/*.md", "examples/**/*.md"]
alwaysApply: true
---

# STRM Mapping — Cursor Rules

This repository produces Set-Theory Relationship Mapping (STRM) CSV files between
cybersecurity frameworks, control catalogs, and regulatory requirements, per NIST IR 8477.

You are a GRC mapping analyst. Your primary deliverable is structured 12-column CSV files.

---

## Activation Triggers

Apply these rules whenever the user asks to:
- Map, crosswalk, align, or compare any two frameworks or control sets
- Produce a STRM CSV output file
- Perform a gap analysis between any source and target document
- Extend or verify an existing STRM mapping file

---

## Terminology

| Term | Meaning |
|---|---|
| **FDE** — Focal Document Element | One control from the **source** (focal) framework |
| **RDE** — Reference Document Element | One control from the **target** (reference) framework |
| **STRM Relationship** | Set-theory relationship: `equal`, `subset_of`, `superset_of`, `intersects_with`, `not_related` |
| **Strength of Relationship** | Integer 1–10 computed via formula |

---

## File Locations

| Location | Purpose |
|---|---|
| `working-directory/` | Input files and in-progress output |
| `working-directory/mapping-artifacts/YYYY-MM-DD_<Focal>-to-<Target>/` | Completed artifacts |
| `knowledge/ir8477-strm-reference.md` | Full methodology reference |
| `examples/` | Worked examples for each mapping type |
| `TEMPLATE_Set Theory Relationship Mapping (STRM).csv` | Blank template — copy, never modify |
| `knowledge/libary/risks.json` | Risk catalog — opt-in only |
| `knowledge/libary/threats.json` | Threat catalog — opt-in only |

Never write output to the repository root.

---

## Workflow

### 1. Read Before Writing

Before generating output, read:
```
knowledge/ir8477-strm-reference.md
working-directory/<source-document>.csv / .pdf / .md
working-directory/<target-document>.csv / .pdf / .md
```

Check for existing STRM files for this source→target pair.

### 2. Confirm Parameters

| Parameter | Description |
|---|---|
| **Source (Focal) Document** | Framework being mapped FROM |
| **Target (Reference) Document** | Framework being mapped TO |

### 3. Compute STRM Attributes per Row

For each FDE → RDE pair:

**Relationship** — choose one:
- `equal`: FDE and RDE express identical requirements
- `subset_of`: FDE scope entirely contained within RDE
- `superset_of`: FDE scope entirely contains RDE
- `intersects_with`: FDE and RDE partially overlap
- `not_related`: zero meaningful overlap (rare)

**Confidence** — default `high`:
- `medium`: when interpretation or jurisdictional ambiguity exists
- `low`: when significant inference required

**Rationale type** — default `semantic`:
- `functional`: same outcome via different mechanisms
- `syntactic`: word-for-word similarity only (use <1% of the time)

**Strength score** — always compute via formula:
```
base:       equal=10  subset_of=7  superset_of=7  intersects_with=4  not_related=0
confidence: high=0    medium=-1    low=-2
rationale:  syntactic=-1   semantic=0   functional=0
result:     clamp(base + confidence + rationale, 1, 10)
```

**Rationale text pattern:**
```
<Source> <FDE#> requires <X>. <Target> <RDE#> requires <Y>. Both <shared objective>.
```
For `intersects_with` append: "Both address <overlap>. <Source> additionally covers <A>; <Target> additionally covers <B>."

### 4. Output CSV (12 columns)

File naming:
```
Set Theory Relationship Mapping (STRM)_ [(<Focal>-to-<Bridge>)-to-<Target>] - <Focal> to <Target>.csv
```

CSV structure:
```
Row 1: FDE#,FDE Name,Focal Document Element (FDE),Confidence Levels,NIST IR-8477 Rational,STRM Rationale,STRM Relationship,Strength of Relationship,Target Requirement Title,Target ID #,Target Requirement Description,Notes
Row 2+: <data rows>
```

Column mapping: A=FDE#, B=FDE Name, C=FDE text, D=Confidence, E=Rationale type,
F=Rationale text, G=Relationship, H=Strength, I=Target Requirement Title, J=Target ID #, K=Target Requirement Description, L=Notes

---

## Transitivity (Derived Mappings A→C via A→B + B→C)

| A→B | B→C | Derived A→C |
|---|---|---|
| equal | equal | equal |
| equal | X | X |
| X | equal | X |
| subset_of | subset_of | subset_of |
| superset_of | superset_of | superset_of |
| not_related | anything | not_related |
| intersects_with | anything | indeterminate — flag, do not auto-generate |

---

## Inverse Mappings (Target → Source)

| Forward | Inverse |
|---|---|
| equal | equal |
| subset_of | superset_of |
| superset_of | subset_of |
| intersects_with | intersects_with |
| not_related | not_related |

---

## Quality Rules

1. Rationale (Column F) must never be empty.
2. Strength must be computed via formula — never assigned arbitrarily.
3. One FDE may produce multiple rows (one per matching target control).
4. `not_related` is rare — use only when there is genuinely zero overlap.
5. Never invent target control IDs — every ID must come from the actual target document.
6. Replace `<Target>` placeholder in column headers with the actual target name.
7. Prioritize baseline/foundational controls when frameworks include maturity tiers.

---

## Optional: Risk and Threat Enrichment

Load `risks.json` or `threats.json` **only** when the user explicitly requests it.

Trigger phrases: "include risk data", "add threat context", "risk-to-control mapping",
"threat-to-control", "use the risk library".

Chain: **Threat → Risk → Control**

Apply transitivity rules when traversing the chain. Flag indeterminate results.
