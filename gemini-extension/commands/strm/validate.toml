prompt = """
Validate existing STRM CSV files in working-directory.

Steps:
1. Run node scripts/bin/strm-list-input-files.mjs --dir working-directory and identify STRM CSV files.
2. For each STRM CSV found (files whose names contain "STRM"):
   a. Run node scripts/bin/strm-validate-csv.mjs --file "<csv path>".
   b. Capture all errors and warnings.
3. Produce a validation report listing:
   - Total rows checked
   - Rows with errors (list each error)
   - Rows with warnings (list each warning)
   - Overall pass/fail status

Current STRM files:
!{find working-directory -name "*.csv" -path "*STRM*" 2>/dev/null || echo "No STRM CSV files found in working-directory"}
"""
